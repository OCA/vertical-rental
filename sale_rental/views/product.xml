<!--
  Copyright 2014-2021 Akretion (http://www.akretion.com/)
  @author Alexis de Lattre <alexis.delattre@akretion.com>
  Copyright 2016-2021 Sodexis
  License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl).
-->
<odoo>
    <record id="product_normal_form_view" model="ir.ui.view">
        <field name="name">rental.product.product.form</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view" />
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button
                    type="object"
                    name="action_view_rental_pricing"
                    class="oe_stat_button"
                    icon="fa-money"
                >
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_text">Rental<br />Pricing</span>
                    </div>
                </button>
            </div>

            <group name="sale" position="inside">
                <label
                    for="rented_product_id"
                    string="Rental"
                    invisible="type != 'service'"
                />
                <field name="rented_product_id" invisible="type != 'service'" />
                <label
                    for="rental_service_ids"
                    string="Rental Services"
                    invisible="type == 'service'"
                />
                <field
                    name="rental_service_ids"
                    nolabel="1"
                    invisible="not rental_service_ids"
                />
                <button
                    type="action"
                    name="%(sale_rental.create_rental_product_action)d"
                    string="Create Rental Service"
                    invisible="type == 'service'"
                />
            </group>
        </field>
    </record>
    <record id="product_template_only_form_view" model="ir.ui.view">
        <field name="name">rental.product.template.only.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <button
                    type="object"
                    name="action_view_rental_pricing"
                    class="oe_stat_button"
                    icon="fa-money"
                >
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_text">Rental<br />Pricing</span>
                    </div>
                </button>
            </div>

            <group name="sale" position="inside">
                <label
                    for="rented_product_tmpl_id"
                    string="Rental"
                    invisible="(type != 'service') or (product_variant_count > 1)"
                />
                <field
                    name="rented_product_tmpl_id"
                    invisible="(type != 'service') or (product_variant_count > 1)"
                />
                <label
                    for="rental_service_tmpl_ids"
                    string="Rental Services"
                    invisible="(type == 'service') or (product_variant_count > 1)"
                />
                <field
                    name="rental_service_tmpl_ids"
                    nolabel="1"
                    invisible="not rental_service_tmpl_ids"
                />
                <button
                    type="action"
                    name="%(sale_rental.create_rental_product_action)d"
                    string="Create Rental Service"
                    invisible="(type == 'service') or (product_variant_count > 1)"
                />
            </group>
        </field>
    </record>
</odoo>
